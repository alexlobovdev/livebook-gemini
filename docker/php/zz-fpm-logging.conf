[global]
error_log = /proc/self/fd/2

[www]
; Include PHP warnings/notices from worker processes in container logs.
catch_workers_output = yes
decorate_workers_output = no

; Keep PHP-FPM access logs in container stderr.
access.log = /proc/self/fd/2
